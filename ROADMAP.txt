
Things still to be done.
------------------------

Lazy create console collection
Log to single log file with port name and hex dump, possible CSV?

To Test
--------------
Change default location of config file
Configure ssh ports through cli.
Add Enabled flag to config
Add 32 port entries to default config file - ttyUSB0 - ttyUSB31
Copy default config file on install to /etc/consoleserver
Configure monitor path and mask through CLI.
add reload port to apply changes to config
Generate server key pair on first run.
require priv to login to master port, plumbing in place just need to decide what is correct

Tested
-------
process_exit

RaspberryPI 10.9.201.3/DHCP
